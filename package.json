{
  "name": "bs-squel",
  "version": "1.0.0",
  "description": "BuckleScript bindings for https://www.npmjs.com/package/squel",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && bsb -make-world",
    "watch": "npm run clean && bsb -make-world -w",
    "clean": "rm -rf lib && bsb -clean-world"
  },
  "keywords": [
    "bucklescript",
    "reason",
    "squel",
    "sql"
  ],
  "license": "MIT",
  "author": "Ryan Biwer",
  "_": [],
  "repository": {
    "type": "git",
    "url": "git@github.com:ryb73/bs-squel.git"
  },
  "peerDependencies": {
    "bs-platform": "^3.1.5"
  },
  "dependencies": {
    "squel": "^5.12.0"
  }
}
